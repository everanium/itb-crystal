name: itb
version: 0.3.1

description: |
  Crystal binding for the ITB Triple Pipeline — a thin proxy over the
  libitb shared library's ITB_Triple_* C ABI (cmd/cshared).

crystal: ">= 1.0.0"

license: MIT
